'use strict';

/**
 * sl-package-experience router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter(
  'api::sl-package-experience.sl-package-experience'
);
