'use strict';

/**
 * sl-package-region service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService(
  'api::sl-package-region.sl-package-region'
);
