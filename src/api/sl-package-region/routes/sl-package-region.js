'use strict';

/**
 * sl-package-region router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter(
  'api::sl-package-region.sl-package-region'
);
