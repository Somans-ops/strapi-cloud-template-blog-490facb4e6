'use strict';

/**
 * sl-package-experience service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService(
  'api::sl-package-experience.sl-package-experience'
);
